import React from 'react';
import {View,
        Image,
        Text,
        ImageBackground,
        TextInput,
        TouchableOpacity
        } from 'react-native';

import ConnectionStyles from './ConnectionStyles';
        const test =() =>{
          return (
 <View style={ConnectionStyles.Container}>
       <Text style={ConnectionStyles.LogoText}>  Bienvenue </Text>
 </View>
            );
          };

          export default test;
